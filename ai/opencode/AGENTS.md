# OpenCode Configuration

Config directory: `~/config/ai/opencode` (via `OPENCODE_CONFIG_DIR`)

## Important Files

- `opencode.jsonc` - Main settings file (JSONC, not JSON)
- `agents/` - Custom agent definitions
- `skills/` - Custom skill definitions
